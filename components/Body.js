import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const Body = (props) => {
    return(
        <View>
            <Text>{props.pd}</Text>
        </View>
    );

}


const styles = StyleSheet.create({
    
});

export default Body;